[project]
name = "telegram-bot-proxy"
version = "0.1.0"
description = ""
requires-python = ">=3.12"
dependencies = [
    "telethon>=1.37.0",
    "cryptg>=0.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
